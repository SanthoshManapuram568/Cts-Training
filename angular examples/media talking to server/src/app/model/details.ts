export class Details{
    title:string;
    des:string;
    tags:string;

    constructor(title:string, des:string, tags:string){
        this.title=title;
        this.des = des;
        this.tags= tags;
    }
}